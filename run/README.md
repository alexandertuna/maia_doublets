# Run directory

This is how I run the code:

```bash
# Go to the run/ directory
cd run/

# Create a virtual environment (once)
python -m venv env

# Activate the environment (on each login)
source env/bin/activate

# pip install the repo
pip install -e ..

# Run!
maia_doublet -h
```
